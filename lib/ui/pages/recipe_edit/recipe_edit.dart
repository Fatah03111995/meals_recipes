import 'package:flutter/material.dart';

class RecipeEdit extends StatelessWidget {
  const RecipeEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
